*.sh text eol=lf
COMMIT_SHA export-subst
# Plik testowy w Windows-1250 z koncami linii CRLF — git nie moze go "naprawiac",
# bo przestalby sprawdzac to, do czego sluzy (patrz docs/przyklady/README.md).
docs/przyklady/*.csv -text
